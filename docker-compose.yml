version: "3.8"

services:
  flarehub:
    build: .
    container_name: flarehub
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - TZ=${TZ:-Europe/Berlin}
